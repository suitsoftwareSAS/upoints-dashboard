# Conexión a PostgreSQL
DB_URI=postgresql://postgres:***@host:port/upoints_prod

# Credenciales del dashboard (opcional — si no se configuran, acceso libre)
DASHBOARD_USER=admin
DASHBOARD_PASS=cambia_esta_contraseña
